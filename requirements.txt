# Eurocode Reader - Phase 1 dependencies
# NOTE: install CPU-only torch first to avoid a ~2.5GB CUDA download:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

pymupdf>=1.24.0
sentence-transformers>=3.0.0
numpy>=1.26.0
customtkinter>=5.2.0

# Phase 5: typesets equation LaTeX into images with its own
# mathtext engine - no LaTeX installation and no network needed.
matplotlib>=3.7.0
